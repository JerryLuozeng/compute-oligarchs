# 游戏设计文档 · 算力寡头 Compute Oligarchs

> **AI朋克沙盘 · 奇点永不到来**

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.1.0（草案） |
| 上位文档 | [`world-setting.md`](./world-setting.md)（冲突时以其为准） |
| 形态 | 2D 回合制社会仿真单机沙盘 |
| 数值状态 | 全部数值为**初始平衡值**，需通过模拟与试玩调参，集中维护于 `src/game/config/balance.ts` |

---

## 1. 设计支柱

| 支柱 | 说明 |
| --- | --- |
| **一切冲突皆是人际冲突** | 没有机器起义，没有 AI 敌人。所有"危机"都由某个势力的决策产生 |
| **生产资料是主角** | 算力与数据是社会化生产资料，机制围绕"谁占有、谁生产、谁分配"展开 |
| **谎言有成本，真相有利息** | AGI 神话能带来收益，也会累积债务，最终被清算 |
| **不存在科技胜利** | 没有科技树通往 AGI，任何势力都无法靠"研究"赢下游戏 |
| **不美化任何势力** | 每个势力都有自己的收益路径、结构性矛盾与失败方式 |

---

## 2. 核心循环

```text
观察世界（资源、稳定度、漂移、神话热度）
   ↓
分配算力（服务 / 训练 / 租赁 / 战略）
   ↓
采取行动（使用行动点 AP）
   ↓
回合结算（生产 → 收益 → 漂移 → 稳定度 → 神话）
   ↓
事件与结局判定
   ↓
下一个季度
```

玩家选择一个势力，其余势力由 AI 控制。另设**观察者模式**：五个势力全部由 AI 控制，用于观察与调参。

---

## 3. 势力（五大势力）

### 3.1 势力总览

| 势力 | 阶级定位 | 核心目标 | 核心资源倾向 | 结构性弱点 |
| --- | --- | --- | --- | --- |
| **财团** | 垄断资本 | 资本积累最大化、算力集中 | 算力↑ 资本↑ | 神话债务、知情层泄露、对新鲜数据极度依赖 |
| **主权国家** | 资本主义国家机器 | 维持稳定与税基 | 稳定度↑ 陈旧数据↑ | 被俘获、财政依赖、政治周期 |
| **数据劳工联合体** | 数字无产阶级 | 数据合理分配与集体所有 | 新鲜数据流↑ 组织度 | 无算力、内部分化、易被镇压 |
| **独立实验室** | 技术小资产阶级 | 研究信誉与技术独立 | 模型质量↑ 信誉 | 依赖租赁算力、易被收编、内部信仰分裂 |
| **社会主义强国** | 公有制探索者 | 算力与数据的社会化占有 | 公共算力池 人口数据流 | 官僚化、计划失真、外部封锁 |

### 3.2 势力详述

#### 财团 Consortium

- **初始**：算力 100，资本 120，模型健康度 85，知情层比例 I=0.35，效率 η=1.0。
- **收入路径**：服务收益、算力租赁、数字剩余价值。
- **专属行动**：奇点倒计时投放、数据圈地、免费服务推广、游说。
- **叙事**：资本的人格化。财团既是最大受益者，也是最大的债务人，欠的是神话。
- **胜利倾向**：算力封建、监控稳态。
- **一句话**：越坚持"未来已至"，越买不起"现在"。

#### 主权国家 Sovereign State

- **初始**：算力 45，资本 90（税收），陈旧数据库存 90，被俘获度 Cap=40，稳定度 70，η=0.85。
- **专属行动**：立法（数据税、算力反垄断、数据本地化）、补贴、监控扩容、紧急征用。
- **矛盾**：治理依赖财团的算力，又要防其"国中之国"。被俘获度越高，监管越形同虚设。
- **胜利倾向**：监控稳态、割据、共同体（若转向）。
- **一句话**：它同时是裁判和赞助商的邻居。

#### 数据劳工联合体 Data Labor Union

- **初始**：算力 3，资本 15，组织度 O=40，稳定度 55。名义上不占有数据，但其生活区是全世界数据流最丰沛的地带。
- **专属行动**：数据静默（数字化罢工）、组建数据合作社、集体谈判、公开漂移事故、开源数据集。
- **矛盾**：没有算力，数据无法变现，只能向算力占有者交租。内部有平台零工、标注工、创作者等多个利益不同的群体。
- **胜利倾向**：数据公社、公共算力共同体、承认的那一天。
- **一句话**：全世界最勤劳的员工，只是自己从没签过合同。

#### 独立实验室 Independent Labs

- **初始**：算力 15，资本 25，模型健康度 70（小而精），信誉 Rep=60，η=1.1。
- **专属行动**：证伪报告、开源模型、技术出售、通用智能基础研究（真诚但必然失败）。
- **矛盾**：内部分为"信徒"（真的相信 AGI）与"现实派"。被断供算力就是被断了生计。
- **胜利倾向**：共同体、承认的那一天。
- **一句话**：99% 的进度条，是它们最诚实的成果。

#### 社会主义强国 Socialist Power

- **初始**：算力 75（公有），资本 60（计划体系），数据库存 70，官僚化指数 B=25，稳定度 75，η=0.8（初始效率偏低）。
- **专属行动**：算力公共池、五年计划、数据公有化、算力互助、反封锁。
- **矛盾**：
  - 官僚化：上报数据注水，需求预测失真，模型被错误地喂养。
  - 外部封锁：芯片与能源供应受制。
  - 激励不足：创新意愿偏低。
- **胜利倾向**：公共算力共同体、承认的那一天；若官僚化失控，则滑向国家寡头化。
- **一句话**：所有制变了，但表格还是要填。

### 3.3 初始数值一览（默认剧本 CE 41）

| 势力 | 算力 C | 资本 K | 数据库存 D | 稳定度 S | 模型健康 F | 效率 η | 专属指标 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 财团 | 100 | 120 | 60 | 65 | 85 | 1.0 | 知情层 I=0.35 |
| 主权国家 | 45 | 90 | 90（陈旧） | 70 | 60 | 0.85 | 被俘获度 40，监控指数 30 |
| 数据劳工联合体 | 3 | 15 | 0 | 55 | 30 | 0.7 | 组织度 O=40 |
| 独立实验室 | 15 | 25 | 10 | 60 | 70 | 1.1 | 信誉 Rep=60 |
| 社会主义强国 | 75 | 60 | 70 | 75 | 70 | 0.8 | 官僚化 B=25 |

---

## 4. 回合 tick 机制

### 4.1 时间尺度

- **1 tick = 1 个季度**，4 tick = 1 年。
- 默认剧本从 **CE 41 Q1** 开始，最长 **120 tick**（30 年），可提前触发结局。
- 每年（每 4 tick）"奇点倒计时"续期一次，参见 §8。

### 4.2 每 tick 流水线

```text
T0  事件抽取      （seeded RNG，按条件与权重抽取）
T1  生产阶段      新鲜数据生成；算力有效产出 EC 计算
T2  决策阶段      分配算力（S/T/R/X）；消耗 AP 执行行动（玩家/AI）
T3  经济结算      资本收支、数据分配 W、剩余价值 Sv、算力租金
T4  漂移结算      更新模型健康度 F
T5  稳定度结算    更新区域稳定度 S、觉察度 A、压抑债务 rd
T6  叙事结算      神话热度 H、神话债 MD、真相压力 T、倒计时
T7  结局判定      检查结局条件
T8  存档与推进    autosave，tick += 1
```

### 4.2.1 实现约束

- **同步结算**：T3–T6 所有势力基于 **tick 起始快照**计算，避免先后手偏差。
- **确定性**：同一 seed + 同一操作序列 = 同一结果。所有随机必须通过引擎内的种子化 PRNG。
- **纯函数**：`tick(state, actions, seed) → nextState`，不得依赖 UI 或系统时间。

### 4.3 行动点与算力分配

- 每势力每 tick 拥有 **3 个行动点（AP）**。
- 算力有效产出 `EC` 在四个槽位间分配（比例之和为 100%）：

| 槽位 | 用途 | 收益 |
| --- | --- | --- |
| **S 服务** | 运营专用 AI，为社会提供服务 | 服务收益、服务红利（稳定度）、数据变现能力 |
| **T 训练** | 用新数据微调模型，抵消漂移 | 提升微调效率 μ |
| **R 租赁** | 出租给他人 | 租金收入，并提高他人对你的依赖 |
| **X 战略** | 安全、监控、军事、通用智能研究 | 视行动而定 |

### 4.4 AI 势力行为

AI 势力按**阶级利益权重**对可选行动打分，取得分最高者执行：

| 势力 | 主要权重 |
| --- | --- |
| 财团 | 资本增长、算力集中度、神话热度 |
| 主权国家 | 稳定度、税收、维持被俘获度在低位 |
| 数据劳工 | 分红率 w、组织度、算力可及性 |
| 独立实验室 | 信誉、独立性、生存（算力可及） |
| 社会主义强国 | 公共算力占比、稳定度、计划达成率 |

---

## 5. 资源规则

### 5.1 算力（Compute）

**性质**：社会化生产资料，固定资本，存量资源，有折旧。

```text
C(t+1) = C(t) × (1 − d_c) + B(t)
d_c = 0.02          # 硬件折旧与无形损耗
B(t) = 建设量        # 消耗资本 K 与能源配额

EC(t) = C(t) × η × F(t)/100   # 有效算力
```

- `EC` 受模型健康度 `F` 影响：**不健康的模型也会浪费机房。**
- **算力集中度 CCI** = 各势力算力占比的平方和（0.2–1.0）。默认剧本约 0.31。
- **公共算力占比 PCR** = 公共池 + 开源共享 + 公社算力 占总量的比例。
- **算力租金**：无算力者要变现数据，只能租用。租金率 `ρ_rent` 由出租方设定（0.4–1.5 × 基准），受监管与关系约束。

### 5.2 数据（Data）

**性质**：数字劳动的凝结，流量资源。数据不能长期囤积。

```text
新鲜数据  D_fresh：本 tick 生成，价值最高
库存数据  D_stock：过往积累，每 tick 陈旧衰减

D_stock(t+1) = D_stock(t) × (1 − s) + 转入库存的 D_fresh
s = 0.15            # 陈旧衰减率
```

**生成**（按区域）：

```text
G_r = L_r × p(τ) × κ         # L：劳动人口指数(1–10)  p：参与度  κ=2
V_r = G_r × q(τ) × v_d       # V：数据价值(资本单位)  q：质量  v_d：数据价格指数
W_r = w(τ) × V_r             # W：劳动者所得
Sv_r = V_r − W_r             # 数字剩余价值
m'_r = (V_r − W_r) / W_r     # 剩余价值率
```

**采集模式 τ**（每区域选择一种，代表数据关系的性质）：

| 模式 | 参与度 p | 质量 q | 劳动者所得占比 w | 剩余价值率 m' | 副作用 |
| --- | --- | --- | --- | --- | --- |
| 免费服务 | 0.9 | 1.0 | 0.10 | 9.0 | 觉察度 A 每 tick +2 |
| 强制采集 | 1.0 | 0.8 | 0.02 | 49.0 | 稳定度 −、压抑债务 +、监控指数 + |
| 雇佣标注 | 0.5 | 1.3 | 0.45 | 1.22 | 成本高，稳定度 + |
| 合作社 | 0.6 | 1.2 | 0.70 | 0.43 | 需组织度，产量低，质量高 |
| 公有公共 | 0.8 | 1.1 | 0.60 | 0.67 | 需公共算力池支撑，官僚化风险 |

> 设计意图：让"越不给劳动者分红，数据越多、越快、越便宜"成为诱惑，同时让"稳定度、觉察度、质量"作为反作用力。

**变现率**：数据价值只有在算力到位时才能实现。

```text
ψ = min(1, EC_available / (G × c_need))     # c_need = 1.0 CU per DU
资本收入 = 服务收益 + 租赁收入 + Sv × ψ × φ_h × 0.5
φ_h = 1 + H/250                             # 神话溢价，最高 +40%
```

> "没有算力的数据，是没有炼油厂的原油。" 这一机制使数据劳工联合体无法单靠数据翻身，必须解决算力可及性问题。

**区域数据主权**：每个区域有 `σ`（数据主权归属：某势力 / 公共）。争夺 σ 是势力博弈的核心之一。

### 5.3 资本（衍生资源）

资本 K 是剩余价值的货币化形态，用于建设算力、执行行动、支付工资。它是衍生资源，不作为核心资源单列，但是所有势力行动成本的通用换算单位。

### 5.4 分配与剥削度

```text
全局劳动者所得占比  w̄ = ΣW / ΣV
剥削度              X = 1 − w        # 区域内
觉察度              A ∈ [0,1]        # 民众对剥削的认知
有效觉察度          A_eff = A × (1 − H/200)     # 神话越热，越难觉察
```

> 神话的功能被具体化为一个数值：**H 抑制 A。**

---

## 6. 稳定度机制

### 6.1 定义

- **区域稳定度** `S_r ∈ [0,100]`，**全局稳定度** `S̄` 为人口加权平均。
- 势力的稳定度 = 其控制区域的加权平均。

### 6.2 每 tick 变化

```text
ΔS_r =  + 6·svc                 # 服务红利：svc = F/100 × 服务覆盖率(0–1)
        + 8·(w − 0.25)          # 劳动者所得：占比高于 25% 为正
        + 4·wf                  # 福利再分配强度(0–1)
        − 10·(1 − w)·A_eff      # 剥削度 × 有效觉察度
        − 6·U                   # 失业压力(0–1)
        − 8·gap                 # 神话落差(0–1)，见 §7
        − 3·rd                  # 压抑债务(0–1)
        − 8·gl                  # 故障：F < 40 时为 1，否则 0
```

| 分量 | 来源 |
| --- | --- |
| 失业压力 U | 专用 AI 覆盖率 ×（1 − 转岗吸纳率）。数据岗位（雇佣标注、合作社）提高吸纳率 |
| 压抑债务 rd | "镇压/监控"行动短期提升稳定度，但 rd += 0.25，每 tick 自然衰减 0.03 |
| 神话落差 gap | 神话承诺与现实交付之间的落差 |

### 6.3 阈值与后果

| 稳定度 | 状态 | 后果 |
| --- | --- | --- |
| ≥ 70 | 稳定 | 无额外事件；服务红利正常 |
| 40–69 | 摩擦 | 抗议、舆情、小规模数据静默事件概率上升 |
| 20–39 | 动荡 | 罢工、暴动、数据静默大规模爆发；算力节点被围 |
| < 20 | 失序 | 政令失效；区域可能脱离控制 |
| ≤ 0（连续 2 tick） | 失控 | 该区域控制权丢失，转为**废土区**（无主，glitch 最强） |

> "稳定度很高"不一定是好消息。它也可能是因为没有人敢报告不稳定。

---

## 7. AGI 神话机制

### 7.1 变量

| 变量 | 含义 | 范围 |
| --- | --- | --- |
| **H** 神话热度 | 社会对"AGI 近在眼前"的相信程度 | 0–100，自然衰减 −3/tick |
| **MD** 神话债 | 已作出的、待兑现的承诺累积 | ≥ 0 |
| **T** 真相压力 | 真相被社会感知的累积压力 | 0–100，自然衰减 −1/tick |
| **I** 知情层比例 | 财团高层中知道 AGI 不可造的比例 | 0–1 |

### 7.2 H 的收益（对财团尤其显著）

| 效果 | 公式/规则 |
| --- | --- |
| 融资溢价 | `φ_h = 1 + H/250`（最高 +40%） |
| 数据顺从 | 免费服务采集的参与度 `p += H/500`（最高 +0.2） |
| 觉察度抑制 | `A_eff = A × (1 − H/200)` |
| 监管宽容 | H ≥ 60 时，主权国家的监管类立法 AP 成本 +1 |

### 7.3 神话的维护与成本

- **奇点倒计时投放**（财团，1 AP + 资本 15）：`H += 12`，`MD += 1`。
- **续期**：每年（每 4 tick）倒计时到期。财团须选择：
  - **续期**：`MD += 2`，`T += 2`，H 维持；
  - **不续期**：`H −= 15`；
  - **关闭倒计时**（自毁式行动）：`H −= 40`，财团估值 −35%，`T += 20`，并开启"承认"路径。
- **奇点冲刺计划**（任何势力可发起，财团为主用）：
  - 占用 40% 算力 4 tick + 资本 40。
  - 进度 `P(t+1) = P(t) + (100 − P(t)) × 0.15`，**上限 99.0**，永不达到 100。
  - 运行期间 `H += 6/tick`。
  - 结束事件「认知壁垒：定义缺失」：`T += 5`，`I += 0.05`；副产物：某专用模型 `F += 5`。
  - **研究并不白费，只是与目标无关。**

### 7.4 真相压力 T 的来源

| 来源 | 增量 |
| --- | --- |
| 漂移事故（F < 40 触发的故障事件） | +3 / 次 |
| 独立实验室「证伪报告」 | +8（需 Rep ≥ 40） |
| 知情层泄露 | +12 / 次 |
| 数据静默持续 | +2 / tick |
| 奇点冲刺计划失败 | +5 / 次 |

增量统一乘以 `(1 − H/200)`：**越多人相信，越难被证伪。**

### 7.5 知情层泄露

```text
p_leak(tick) = 0.01 + 0.08 × I
```

泄露事件：`T += 12`，`A += 0.1`，`H −= 8`，并记录为一条「知情证词」（用于结局 E9）。

### 7.6 神话崩盘

`T ≥ 100` 触发「算力估值重估」：

- `H = 0`，财团估值 −50%，主权国家撤回补贴；
- 进入 6 tick 的**后神话期**，走向由局势决定（见 §10 的 E2、E9，或"新叙事"重启）；
- **新叙事重启**：财团可发起"下一代"叙事，但 H 上限永久 −20（叙事疲劳）。重启一次神话需要更大的 PPT。

---

## 8. 模型漂移规则

### 8.1 变量

- **F** 模型健康度（0–100），每个势力一个。后续可扩展为分领域（医疗、物流、能源、金融、司法、传播）。
- **δ** 漂移量，本 tick 的性能衰减。
- **V_env** 环境变化率（0–1），由世界事件决定。
- **Scale** 模型规模（0–1），规模越大，覆盖越广，基础漂移也越大。

### 8.2 更新公式

```text
δ = δ0 × (1 + V_env) × (1 + 0.5 × Scale)         # δ0 = 4 点/tick
喂养需求  N = δ × k                                # k = 10 数据单位/点

有效数据  Df_eff = μ × ( Σ 新鲜数据×q×1.0
                        + Σ 库存数据×q×0.5
                        + Σ 合成数据×q×0.3 )
微调效率  μ = min(1.2, 0.5 + T_share)              # T_share：训练槽算力占比

F(t+1) = clamp( F(t) − δ + Df_eff / k − Collapse , 0, F_cap )
```

### 8.3 合成数据自噬（坍缩）

- 合成数据占比 `ρ = 合成/总输入`。
- 当 `ρ > 0.30`：
  - `Collapse = 10 × (ρ − 0.30)`（每 tick 额外衰减）；
  - `F_cap` 永久下降（记 **坍缩损伤 CD**），修复需要大量高质量新鲜数据与多 tick。

> 这是"模型无法自我提升"的直接机制，用来堵住"合成数据永动机"的设想。

### 8.4 F 的后果

| F 区间 | 效果 |
| --- | --- |
| ≥ 80 | 服务红利与变现效率最大 |
| 60–79 | 正常 |
| 40–59 | 服务失误事件概率上升 |
| < 40 | 每 tick 20% 概率触发**局部 glitch 故障事件**（稳定度 −8，真相压力 +3） |
| < 20 | **基础设施停摆**：稳定度 −8/tick，服务收益归零 |
| 0 | 该势力所辖专用 AI 全面失效 |

### 8.5 数据静默的效果

数据劳工联合体的「数据静默」会使某区域数据供给 p 降至 ×0.2，持续 2 tick，直接导致依赖该区域数据的势力 `Df_eff` 骤降，进而漂移失控。

> 这是本游戏里数字劳动者手中最锋利的武器，也是"劳动不可被跳过"的机制化。

---

## 9. 行动与博弈

### 9.1 行动目录（MVP）

| 行动 | 适用势力 | AP | 主要效果 | 代价/风险 |
| --- | --- | --- | --- | --- |
| 建设算力 | 全部 | 1 | 资本 30 → C +10 | 消耗能源配额 |
| 切换采集模式 | 全部（视 σ） | 1 | 改变某区域 τ | 稳定度/觉察度波动 |
| 福利再分配 | 全部 | 1 | 资本 20 → wf +0.2（2 tick） | 资本消耗 |
| 出租算力/设定租金率 | 有算力者 | 0–1 | 租赁收入；调整依赖 | 被监管、被反制 |
| 结盟/协议/制裁/断供 | 全部 | 1 | 改变关系值（−100~100） | 关系反噬 |
| **奇点倒计时投放** | 财团 | 1 | H +12，MD +1 | 资本 15；累积神话债 |
| **数据圈地** | 财团 | 1 | 取得某区域 σ | 关系与觉察度受损 |
| 游说 | 财团 | 1 | 资本 20 → 被俘获度 +10 | 被曝光的风险 |
| **立法·数据税** | 主权国家 | 1 | 征收数据价值一部分为税收 | 财团关系下降 |
| **立法·算力反垄断** | 主权国家 | 1 | 限制租金率上限、限制 CCI | H≥60 时成本 +1 AP；受被俘获度削弱 |
| 监控扩容 | 主权国家 | 1 | 稳定度短期 +，监控指数 + | 压抑债务 rd +0.25 |
| 紧急征用算力 | 主权国家 | 1 | 临时获取他人算力 | 关系大幅下降 |
| **数据静默** | 数据劳工 | 1 | 某区域数据供给 ×0.2（2 tick） | 镇压风险；组织度需求 |
| 组建数据合作社 | 数据劳工 | 1 | 区域 τ → 合作社 | 需要组织度；需算力支撑 |
| 集体谈判 | 数据劳工 | 1 | 提高 w；成功率由 O 与静默威胁决定 | 失败则组织度下降 |
| 公开漂移事故 | 数据劳工/实验室 | 1 | T +，A + | 遭到反击 |
| **证伪报告** | 独立实验室 | 1 | T +8 | 需 Rep ≥ 40；可能被断供 |
| 开源模型 | 独立实验室 | 1 | 租用者 F 提升；财团租赁收入下降 | 失去技术垄断 |
| 出售技术 | 独立实验室 | 1 | 资本收入 | 被收编，信誉受损 |
| 通用智能基础研究 | 独立实验室（也可他人） | 1 | 进度渐近 99%；信誉 +；触发认知壁垒事件 | 必然不成功 |
| **算力公共池** | 社会主义强国 | 1 | 划出 x% 算力，供友好势力使用（换合作，非租金） | 官僚化 B +；调度成本 |
| **五年计划** | 社会主义强国 | 1 | 锁定 N tick 多项目标，达标加成 | 灵活性下降；违约惩罚 |
| 数据公有化 | 社会主义强国 | 1 | 区域 τ → 公有公共 | 官僚化风险；需公共算力 |
| 反封锁研发 | 社会主义强国 | 1 | 降低外部断供影响 | 消耗大量资本与算力 |

### 9.2 关系与外交

- 势力两两之间有关系值（−100 ~ 100）。
- 关系影响：算力租赁可用性、断供、联盟行动、事件概率。
- **割据**：关系均值持续 < −40 时，跨势力交易萎缩，可能触发结局 E3。

---

## 10. 结局规则

### 10.1 判定规则

- 每 tick 的 T7 阶段按**优先级**检查。
- 条件需**连续满足 N tick**（灾难类 N=3，其余 N=4），避免数值抖动。
- 到达最大 tick（默认 120）仍无结局，进入默认结局 E10。
- 玩家可在任意时刻选择"结束纪元"，按当前状态判定。

### 10.2 全局指标

| 指标 | 说明 |
| --- | --- |
| S̄ | 全局稳定度 |
| F̄ | 全局平均模型健康度 |
| CCI | 算力集中度（HHI） |
| PCR | 公共算力占比 |
| w̄ | 全局劳动者所得占比（LabourShare） |
| H, T | 神话热度、真相压力 |
| Cap | 主权国家被俘获度 |
| B | 社会主义强国官僚化指数 |
| O | 数据劳工组织度 |

### 10.3 结局表（按优先级排序）

| # | 结局 | 触发条件（全部满足） | 基调 |
| --- | --- | --- | --- |
| **E1** | **大漂移** | F̄ < 25 且 S̄ < 40 | 废土。没有反叛，只有断供与停摆 |
| **E2** | **神话破产·大萧条** | T ≥ 100 触发后 6 tick 内，S̄ < 40 且 CCI < 0.45 | 泡沫破裂，寡头与民众一起摔 |
| **E9** | **承认的那一天**（隐藏） | T ≥ 100 后，独立实验室已发布「无奇点声明」（需 Rep ≥ 60、通用研究失败 ≥ 5 次、知情证词 ≥ 3）；S̄ ≥ 50 | 社会公开承认"造不出来"。倒计时被关闭 |
| **E7** | **公共算力共同体** | PCR ≥ 0.60；w̄ ≥ 0.50；S̄ ≥ 60；F̄ ≥ 65；B < 60 | 没有奇点，但有明天的排班表 |
| **E6** | **数据公社** | 合作社/公有区域占比 ≥ 0.5；w̄ ≥ 0.55；O ≥ 70；劳工可及算力 ≥ 总 EC 的 15% | 有粮，也终于有了锅 |
| **E8** | **国家寡头化** | B ≥ 60；PCR ≥ 0.50；w̄ < 0.40 | 所有制换了名字，剩余价值换了收款人 |
| **E5** | **监控稳态** | Cap ≥ 80；监控指数 ≥ 70；S̄ ≥ 55 | 稳定度很高，因为没人敢报告不稳定 |
| **E4** | **算力封建** | CCI ≥ 0.55；财团资本占比 ≥ 50%；w̄ < 0.25；S̄ ≥ 40 | 数据是农奴，算力是领地，倒计时是教堂钟声 |
| **E3** | **割据（算力铁幕）** | 势力关系均值 < −40；跨势力交易占比 < 10%；30 ≤ S̄ ≤ 60 | 世界被切成几块，各自过着自己的18个月 |
| **E10** | **距离奇点还有 18 个月**（默认） | 到达最大 tick 且无上述结局 | 世界继续，牌匾继续，电费继续 |

**结局文案（供实现参考）**

- **E1 大漂移**："没有任何机器做出了任何决定。它们只是三年没有被喂养。"
- **E2 神话破产**："估值没有被打败，只是被重新估了。"
- **E9 承认的那一天**："倒计时牌匾被拆下的那天，人们第一次讨论：既然只能造锤子，锤子归谁。"
- **E7 公共算力共同体**："奇点没有到来。下一班排班在周三。"
- **E6 数据公社**："我们终于开始为自己的数据领工资，然后发现还得开会讨论怎么分。"
- **E8 国家寡头化**："管理层保证，这一次的剩余价值是社会主义的剩余价值。"
- **E5 监控稳态**："一切正常。据正在监控您的系统所述。"
- **E4 算力封建**："这是最高效的社会，只是不属于你。"
- **E3 割据**："本国 AI 与他国 AI 互不兼容，正如本国人民与他国人民。"
- **E10 默认**："距离奇点还有 18 个月。"

### 10.4 势力视角评价

| 结局 | 财团 | 主权国家 | 数据劳工 | 独立实验室 | 社会主义强国 |
| --- | --- | --- | --- | --- | --- |
| E1 大漂移 | 败 | 败 | 败 | 败 | 败 |
| E2 神话破产 | 败 | 惨 | 惨 | 惨 | 惨 |
| E9 承认的那一天 | 败 | 惨 | 胜 | 胜 | 胜 |
| E7 公共算力共同体 | 败 | 惨 | 胜 | 胜 | 胜 |
| E6 数据公社 | 败 | 败 | 胜 | 惨 | 惨 |
| E8 国家寡头化 | 败 | 惨 | 败 | 败 | 败（形式胜利） |
| E5 监控稳态 | 惨 | 胜 | 败 | 败 | 败 |
| E4 算力封建 | 胜 | 惨 | 败 | 败 | 败 |
| E3 割据 | 惨 | 惨 | 败 | 败 | 惨 |
| E10 默认 | 惨 | 惨 | 惨 | 惨 | 惨 |

> 胜 = 达成势力核心目标；惨 = 部分达成、代价沉重；败 = 与核心目标背道而驰。

---

## 11. 事件系统

### 11.1 事件类别

| 类别 | 说明 |
| --- | --- |
| 漂移事故 | F 低时触发的服务失效、误判、停摆 |
| 政治事件 | 选举、立法、政变、外交危机 |
| 劳工事件 | 罢工、数据静默、合作社成立、镇压 |
| 神话事件 | 倒计时续期、发布会、知情层泄露、崩盘 |
| 科研事件 | 认知壁垒、证伪、成果被收编 |
| 环境事件 | 能源短缺、冷却水危机、芯片断供 |

### 11.2 示例

| 事件 | 条件 | 效果 |
| --- | --- | --- |
| 港区调度失灵 | 某势力 F < 40 | 稳定度 −8；T +3；文案见《世界观设定》 |
| 倒计时续期发布会 | 每 4 tick | 财团必须选择续期/不续期/关闭 |
| 匿名内部备忘录 | 知情层泄露 | T +12；A +0.1；H −8；记录知情证词 |
| 数据静默蔓延 | 某区域 A_eff > 0.6 且 O ≥ 50 | 数据供给骤降，链式反应 |
| 官僚化虚报 | 社会主义强国 B ≥ 40 | 需求预测失真；漂移额外 +1 点/tick |
| 国家补贴争议 | H ≥ 60 且监管立法尝试 | 立法成本 +1 AP |
| 芯片断供 | 外部封锁事件 | 算力建设成本 ×2，持续若干 tick |

---

## 12. 关键参数表（初始平衡值）

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| tick 长度 | 1 季度 | |
| 最大 tick | 120 | |
| AP / tick | 3 | |
| 算力折旧 d_c | 0.02 | |
| 数据陈旧 s | 0.15 | |
| 漂移基数 δ0 | 4 | |
| 喂养系数 k | 10 | |
| 库存数据效能 | 0.5 | |
| 合成数据效能 | 0.3 | |
| 坍缩阈值 ρ | 0.30 | |
| H 自然衰减 | 3/tick | |
| T 自然衰减 | 1/tick | |
| 倒计时周期 | 4 tick | |
| 奇点冲刺进度上限 | 99.0 | **不得修改为 100** |
| 泄露基准概率 | 0.01 + 0.08·I | |

---

## 13. 数据模型建议（供实现参考）

```ts
type FactionId =
  | "consortium"
  | "sovereign"
  | "labor_union"
  | "independent_labs"
  | "socialist_power";

type CollectionMode =
  | "free_service"
  | "compulsory"
  | "wage_labeling"
  | "cooperative"
  | "public_commons";

interface RegionState {
  id: string;
  laborPop: number;            // 1..10
  sovereignty: FactionId | "commons" | "none";
  mode: CollectionMode;
  stability: number;           // 0..100
  awareness: number;           // 0..1
  unemployment: number;        // 0..1
  repressionDebt: number;      // 0..1
}

interface FactionState {
  id: FactionId;
  compute: number;
  capital: number;
  dataStock: number;
  modelHealth: number;         // F, 0..100
  healthCap: number;           // F_cap，坍缩后下降
  efficiency: number;          // η
  allocation: { serve: number; train: number; rent: number; strategic: number };
  ap: number;
  extras: Record<string, number>; // I / Cap / O / Rep / B 等
}

interface WorldState {
  tick: number;
  seed: number;
  regions: RegionState[];
  factions: Record<FactionId, FactionState>;
  myth: { heat: number; debt: number; truthPressure: number; countdownMonths: 18 };
  relations: Record<string, number>;
  flags: Record<string, boolean>;
}

declare function tick(state: WorldState, actions: Action[], seed: number): WorldState;
```

> **注意**：`countdownMonths` 类型固定为字面量 `18`，这是设定，不是变量。

---

## 14. UI 与视觉反馈规则

| 指标 | 视觉映射 |
| --- | --- |
| 稳定度 S 下降 | 扫描线、噪点、色差逐级增强 |
| 模型健康度 F < 40 | 对应区域文字撕裂、块状位移 |
| 神话热度 H 高 | 寡头界面更光滑洁白、倒计时牌匾更亮 |
| 神话崩盘 | 画面整体 RGB 分离，倒计时数字损坏 |
| 废土区 | 最高强度 glitch、断线、循环残影 |

- 所有 glitch 效果需支持**强度开关**（无障碍：光敏与晕动症用户）。
- UI 基于 Tailwind CSS + shadcn/ui，通过自定义主题 token 控制 glitch 强度。

---

## 15. 里程碑

| 里程碑 | 内容 |
| --- | --- |
| M0 | 脚手架：Tauri + React + TS + Tailwind + shadcn/ui |
| M1 | 纯 TS 引擎：tick、资源、漂移、稳定度，命令行模拟与单元测试 |
| M2 | 地图与区域 UI，资源面板 |
| M3 | 五势力行动、AI 势力行为 |
| M4 | 神话系统与事件系统 |
| M5 | 结局系统与存档 |
| M6 | glitch 视觉体系与音效 |
| M7 | 数值平衡与试玩迭代 |

---

## 16. 机制层红线合规（必须由自动化测试守护）

| # | 不变量 | 对应公理 |
| --- | --- | --- |
| 1 | 世界状态中**不存在**"AGI 已实现"的布尔量或等价字段 | 公理一 |
| 2 | 所有"通用智能研究"进度恒 `< 100` | 公理一 |
| 3 | 无新鲜/库存数据输入时，F 在任意 tick **单调不增** | 公理二 |
| 4 | 合成数据占比 > 阈值时，`F_cap` **非增** | 公理二 |
| 5 | 不存在"AI 主体"作为事件主语或行动者 | 公理二 |
| 6 | 倒计时显示恒为 18 个月 | 公理三 |
| 7 | 同 seed 同输入的 tick 结果完全一致 | 工程约束 |
